# Minimal config. The database URL and metadata come from alembic/env.py
# (which reads the app settings), so nothing here needs editing.
[alembic]
script_location = alembic
prepend_sys_path = .
version_path_separator = os
